server.port: 9999
management.endpoints.web.exposure.include: hystrix.stream

#------------------------- Eureka Service Discovery config's -------------------------------      
eureka:
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
