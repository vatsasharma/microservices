package com.sharm.customerservice.events;

public class SimpleSourceBean {

	// private Source source;
	//
	// private static final Logger logger =
	// LoggerFactory.getLogger(SimpleSourceBean.class);
	//
	// @Autowired
	// UserContext userContext;
	//
	// @Autowired
	// public SimpleSourceBean(Source source) {
	// this.source = source;
	// }
	//
	// public void publishOrgChange(String action, String orgId) {
	// logger.debug("Sending Kafka message {} for Organization Id: {}", action,
	// orgId);
	// OrganizationChangeModel change = new
	// OrganizationChangeModel(OrganizationChangeModel.class.getTypeName(),
	// action, orgId, userContext.getCorrelationId());
	// source.output().send(MessageBuilder.withPayload(change).build());
	// }

}
